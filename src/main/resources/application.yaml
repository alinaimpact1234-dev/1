spring:
  application:
    name: lessons

  datasource:
    url: jdbc:postgresql://localhost:5432/test
    username: postgres
    password: 12345
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

  sql:
    init:
      mode: always